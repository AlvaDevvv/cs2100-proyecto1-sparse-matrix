Nombre del proyecto y breve descripción.

Las filas y columnas comienzan en 0. La conversión de notación hoja de cálculo (A1, B3, AA12) a índices numéricos se hará en el parser/GUI.

Lista de métodos públicos con una frase de descripción.

Instrucciones para compilar desde cero (cmake -B build && cmake --build build).

Estado actual.
